# Ports: backend 3000, admin dashboard 3001, public-web 3002

# Public-web origin (for waitlist form posting to same origin)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3002

# Backend (Nest) for server-side proxy (waitlist). Backend runs on 3000.
BACKEND_URL=http://127.0.0.1:3000
