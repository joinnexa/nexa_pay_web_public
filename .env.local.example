# Ports: backend 3000, admin dashboard 3001, stays-web 3002, pay-web 3003

# Backend API base URL (Nest)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# Backend (Nest) for server-side proxy (waitlist). Backend runs on 3000.
BACKEND_URL=http://127.0.0.1:3000
